prim print :: x
prim delay :: x

fn fib :: n =>
  if n < 2 then n
  else (fib (n - 1)) + (fib (n - 2))

fn main :: x =>
  mut i = 0 in
  while true => (
    print (fib *i);
    !i = *i + 1;
    delay 500
  )
